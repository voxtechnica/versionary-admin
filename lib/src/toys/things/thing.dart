/// A placeholder class that represents an entity or model.
class Thing {
  int id;
  String title;
  bool isComplete;

  Thing({
    this.id = 0,
    this.title = '',
    this.isComplete = false,
  });
}
